{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if grep -qE \"git +commit\" <<< \"$CLAUDE_TOOL_INPUT\"; then cd \"C:/Software Dev/ozdemir-makine-web\" && npm run lint || { echo \"KAPI: lint hatasi, commit engellendi\" >&2; exit 2; }; fi'"
          }
        ]
      }
    ]
  }
}
