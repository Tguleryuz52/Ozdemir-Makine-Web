# ⏭️ CONTINUE HERE — Oturum Devir (2026-09-14 → 2026-09-15 Salı)

> `/clear` sonrası ilk oku (STATE.md + CLAUDE.md ile).

## Bugün ne bitirdik (2026-09-14 Pazartesi)
✅ **Faz 8 — Backend & Ölçüm TAMAM.** Detay: `.planning/specs/faz-8-backend-olcum.md`.
- Sanity Webhook route (`/api/revalidate`)
- Zoho CRM Lead entegrasyonu (`/api/lead` + `src/lib/zoho/`)
- Resend mail (kullanıcı teşekkür + ofis bildirim)
- KVKK banner + Gizlilik sayfası
- GA4 (consent-first) + UTM localStorage yakalama
- Rate-limit + honeypot
- **Kampanya UI** (`?kampanya=<slug>` → küçük kart + Zoho'ya `Website — <title>` kaynak)
- **Zoho Description formatı** — emojisiz ASCII, tıklanır makine sayfa linki
- Sanity 6.12→6.13.2 upgrade (React 19 warning fix, hediye)
- Zoho Custom View "Websiteden Gelenler" (Talha kurdu)
- Zoho MCP bağlantısı (Talha bağladı, session'da aktif)

## Yarın (2026-09-15 Salı) — GÖREV LİSTESİ

### Faz 9 — Sanity ↔ Zoho Products SYNC (~1 saat)
**Amaç:** Yeni Sanity makinesi → otomatik Zoho Products → CRM lead'de dropdown'da seçili gelsin.

1. **Sanity API token yenile** (Talha) — bugünkü scriptte 401 aldı, muhtemelen rotate edilmiş. sanity.io/manage → Tokens → yeni bir "Read" token üret → `.env.local`'deki `SANITY_API_READ_TOKEN` değiştir.
2. **Zoho refresh token scope genişlet** (Talha) — `api-console.zoho.eu` → Grant Code scope ekle: `ZohoCRM.modules.products.CREATE,ZohoCRM.modules.products.READ` → `npx tsx scripts/_zoho-refresh.ts <code>` ile yenile.
3. **Zoho Lookup field oluştur** (Talha manuel, ~3 dk) — Ayarlar → Modüller ve Alanlar → Müşteri Adayları → Alanlar → **+ Alan Oluştur** → tip **"Arama"** (Lookup) → Ad `İlgilendiği Makine (Website)` → Aranan Modül **Ürünler**. API name'ini bana ver.
4. **Ben: sync script** (`scripts/sync-sanity-to-zoho.ts`) — Sanity'den tüm makineleri çek, Zoho Products'a upsert (Product_Code = urunKodu).
5. **Ben: `/api/revalidate` webhook'una upsert de bağla** — `_type == "machine"` gelince Products'a da yaz.
6. **Ben: `insertLead()` — Products search + ID → yeni Lookup field'a ata.**
7. **Test:** yeni makineyle test lead → Zoho'da dropdown'da makine adı seçili görünsün.

### Faz 10 — Vercel Deploy (~1 saat)
- Vercel repo bağla (yok ise import), env değişkenlerini gir (`.env.local`'deki hepsi + Sensitive işaretle).
- **⚠️ Domain cutover YAPMA** — sadece `<name>.vercel.app` URL'inde canlı. `ozdemirmakine.com.tr` DNS'ine kesinlikle dokunma.
- Sanity Manage → API → CORS → `https://<name>.vercel.app` ekle.
- Sanity Manage → API → Webhooks → Create URL `https://<name>.vercel.app/api/revalidate` + secret.
- Resend `ozdemirmakine.com.tr` domain doğrulama (SPF/DKIM/DMARC DNS kayıtları — Talha domain hostingine ekler).
- **Prod mail:** `MAIL_FROM=info@ozdemirmakine.com.tr` env update.
- Test: `<name>.vercel.app`'te form doldur → CRM lead + gerçek mail.

## Ortam Notları
- **autocheck.cjs hook BOZUK** (yol boşluğu → `'C:\Software' is not recognized`). YOKSAY. Elle: `npx tsc --noEmit` + `npx eslint <dosya>`.
- Dev: `npm run dev` → localhost:3000. `/studio` gömülü.
- **`.env.local` ASLA commit edilmez** (git-ignored `.env*`). Zoho + Sanity + Resend + GA + Webhook token'ları burada.
- GitHub: `Tguleryuz52/Ozdemir-Makine-Web`, branch `feat/homepage-sections-and-project-hardening` push'lu.
