# ⏭️ CONTINUE HERE — Oturum Devir (2026-09-08)

> `/clear` sonrası ilk oku (STATE.md + CLAUDE.md ile). Kaldığımız yer, karar, bekleyenler.

## Neredeyiz
- **Faz 1 (Keşif):** ✅ Canlı site + himon analiz. `design/references/himon-analysis.md`, ekranlar `design/references/{himon,canli-site}/`.
- **Faz 2 (Design System):** ✅ Token'lar kilitli. `design/design-system.md` + `globals.css`. Demo: `/design-system` (geçici). SUMMARY yazıldı.
- **Faz 3 (Global Layout):** 🚧 **HEADER üzerinde çalışıyoruz.** Footer henüz yok.

## Header durumu (`src/components/layout/header.tsx`)
- himon dili: beyaz zemin, logo solda (`max-w-[104rem]`, `px-4 lg:px-8`), nav ortada 6 item (**Galeri header'dan silindi**, footer'a).
- Nav: Geist, `text-base`, tracking -0.015em, hover'da `text-ink/70` + **soldan-sağa altı-çizili** animasyon (`::after` scaleX, 550ms, ease-out-soft).
- **Teklif Al butonu:** 21st.dev `arrow-fill-button` (`src/components/arrow-fill-button.tsx`) kullanıyor, `HEADER_BTN_SIZE` ile ~44px'e sabitlendi. Renk: siyah pill → hover'da beyaz dolum + ok, metin koyulaşır.
- **Logo:** GEÇİCİ (siyah mark + "ÖZDEMİR MAKİNE"). **Talha gerçek logoyu atacak** → Wordmark() değişecek.

## BEKLEYEN İŞLER (sıra)
- ✅ **arrow-fill-button refactor YAPILDI:** vw yerine CSS değişkenli, boyut-bağımsız (`src/components/arrow-fill-button.tsx`). Varsayılan ~48px header ölçeği; hero için `[--afb-h:...]` ile büyüt. Daire taşması + hover "Al" kırpması + beyaz dolumun yayılmaması (inline inset → class, `group-hover:inset-0` ezebilsin) ÇÖZÜLDÜ. 21st orijinaliyle aynı. tsc/eslint/build temiz.
- ✅ **Header himon'a oturtuldu:** beyaz zemin, logo sol, nav ortada 6 item, altı-çizili hover, ~48px Teklif Al arrow-fill butonu.
1. **Footer** (SIRADAKİ) — himon dili: monospace kolonlar (Site Haritası + Galeri, İletişim, Adres TR+DE, Sosyal) + dev **ghost "ÖZDEMİR" wordmark** + legal bar. İçerik `src/content/site.ts`'te hazır (email, phone, address, socialLinks).
2. **gsd-ui-review** çalıştır (header+footer bitince, anti-slop kapısı).
3. **Gerçek logo** entegrasyonu (Talha atınca) → `Wordmark()` değişecek.
4. (İnce ayar) Header'da hero scroll davranışı: hero yapılınca (Faz 4) şeffaf-üstünde + beyaz yazı, kaydırınca beyaz zemin.

## KİLİTLİ KARARLAR
- **Frontend-önce, Sanity EN SON (Faz 9).** İçerik önce tipli mock (`src/content`).
- **Aksan: Özdemir mavisi `#234D9C`** (deep `#164295`, bright `#0E92DD`). **Font: Geist + Geist Mono** (himon da Geist — birebir).
- **Referans: himon.framer.website** (dil alınır, kod alınmaz). **İçerik kaynağı: canlı site** (zip kilitli, `1234` tutmadı).
- himon header ölçüleri: nav 16px/500/-0.32em, gap ~25px, buton 40px siyah pill, full-width ~50px yanal boşluk.
- Sepet/üye girişi: header'da GİZLİ (v1), yapıda yer var.

## ORTAM NOTLARI (token tasarrufu için önemli)
- **autocheck.cjs hook BOZUK:** yoldaki boşluk yüzünden her Edit'te sahte `'C:\Software' is not recognized` hatası verir. **YOKSAY.** Doğrulamayı elle yap: `npx tsc --noEmit` + `npx eslint <dosya>`. Düzeltme classifier tarafından engellendi (dosya `~/.claude/hooks` altında, proje dışı).
- **21st.dev API anahtarı** sohbette verildi (Talha). Component çekmek için: `API_KEY_21ST=<key> npx shadcn@latest add "<url>?api_key=<key>"`. **Repoya yazma.** MCP eklemek istenirse: `claude mcp add --transport http 21st https://21st.dev/api/mcp --header "x-api-key: <key>"`.
- **IT engeli:** sistem installer (winget/exe) çalışmaz. Sadece npm/npx/node.
- **Dev:** `npm run dev` → localhost:3000. **Tasarım işinde ZORUNLU:** `ui-ux-pro-max` skill + `design/references/`.
- **GSD:** kurulu (gsd-sdk v1.42.3, global). Komutlar tire ile (`/gsd-plan-phase`), iki nokta değil.

## SIRADAKİ SOMUT ADIM
Header ✅ onaylandı. **Sıradaki: FOOTER** (himon dili). İçerik `src/content/site.ts` hazır.

## 🆕 YENİ KURULAN YETENEKLER (2026-09-08)
Üç skill `.claude/skills/` altında, repoda. Tasarım akışı artık **4 kapı** — detay `design/design-system.md` sonundaki "Tasarım İş Akışı" bölümü, özet CLAUDE.md'de.
1. `design-taste` (klonlandı, h3nryprod01/design-taste) — zevk kapısı. Design Read + 3 dial + anti-slop + motion + pre-flight. Dial varsayılanları: VARIANCE 4 / MOTION 5 / DENSITY 3.
2. `reference-parity` (elle yazıldı) — himon'dan TEK bölüm çıkarma + **Playwright ile tarayıcıda çek-bak-düzelt döngüsü** (1440x900 + 390x844).
3. `web-interface-guidelines` (Vercel kurallarından yazıldı) — doğruluk kapısı, üç sütunlu tablo, onaysız düzeltme yok.
- **screenshot-to-code KURULDU** (Talha istedi, 2026-09-08). Yer: `C:/Software Dev/tools/screenshot-to-code` — **repo dışı**, Next.js projesini kirletmiyor. Poetry/Docker olmadan kuruldu: backend `.venv` + pip, frontend `npm install`. İkisi de smoke-test edildi ✅ (uvicorn 7001, vite 5173). **API anahtarı eksik** — `backend/.env` içine `ANTHROPIC_API_KEY` yazılmalı. Kullanım: `tools/screenshot-to-code/BASLA.md`. **Rolü: sadece ölçü çıkarma** (grid/boşluk/font oranı). Çıktı HTML'i bu repoya asla kopyalanmaz, `reference-parity` A2b adımında geçiyor.
- **DOSYA YAPISI TEMİZLENDİ:** kökteki 12 `ozdmak-*.png` → `.work/shots/arsiv/`, `screen/` → `design/inbox/`, `.playwright-mcp/` → `.work/playwright/`. `.gitignore` güncellendi. Kural: geçici her şey `.work/` (git dışı), Talha'nın attığı görseller `design/inbox/`, kalıcı olan `design/references/`. Ekran görüntüsü adı `<bolum>-<varyant>-<viewport>.png`. Detay: `.planning/codebase/STRUCTURE.md` → "Dosya disiplini".
- **GitHub remote eklendi:** `origin` → `https://github.com/Tguleryuz52/Ozdemir-Makine-Web.git`. **HENÜZ PUSH EDİLMEDİ** — Talha "yapı tam temizlenince toplu ve temiz push" dedi.
- (eski not) screenshot-to-code önce kurulmamıştı; Ayrı bir FastAPI+React uygulaması; Docker ve Poetry bu makinede yok, ayrıca çıktısı standalone HTML olurdu → projenin "ham değer/kopya kod yasak" kuralını ihlal ederdi. İşlevi `reference-parity` içine gömüldü (referans PNG'yi doğrudan okuyup token'a çevirme).
- **Playwright zaten kuruluydu** (MCP plugin) — yeni olan, kullanımının kural hâline getirilmesi.

## NOT (yeni oturum)
- Talha `/clear` sonrası **yeni skill/yetenek yükleyecek** — yeni araçlar görünebilir, listeye bak.
- Header taslağı Talha tarafından da düzenlenebiliyor (px/gap/nav boyutu); dosyayı düzenlemeden önce oku.
- Gerçek logo hâlâ bekleniyor (Talha atacak).
