# ⏭️ CONTINUE HERE — Oturum Devir (2026-09-13 → Pazartesi planı)

> `/clear` sonrası ilk oku (STATE.md + CLAUDE.md ile). Kaldığımız yer + yarının somut görevleri.

## Neredeyiz
- **Faz 1-6:** ✅ TAMAM (keşif, design system, layout, ana sayfa, katalog+detay, iletişim).
- **Faz 7 (Sanity CMS):** ✅ **Kod tarafı bitti** — Makineler + Blog + Galeri + Site Ayarları Studio'dan yönetiliyor, siteye bağlı, build+lint temiz, GitHub'da (`feat/homepage-sections-and-project-hardening`, commit `19e8698`). Graphify haritası güncel (905 node).
- **KALAN = canlıya çıkış.** Sanity'yi "resmen paketlemek" için son adım: deploy + webhook + gerçek içerik testi.

## ⏭️ PAZARTESİ GÖREV LİSTESİ (sırayla — ~2-3 saat)

### 0) Isınma (5 dk) — ben
`npm run dev` → `/studio`, `/blog`, `/galeri`, `/iletisim` hızlı göz. Her şey yerinde mi.

### 1) PR & main'e birleştir — ben açarım, Talha onaylar
- `feat/homepage-sections-and-project-hardening` → `main` PR. Vercel prod'u `main`'den deploy edeceği için önce burası.
- Talha PR'a bakar, "tamam" der → merge.

### 2) Vercel deploy — Talha (hesap) + ben (rehber)
- Vercel'de repo bağlı mı bak; değilse **Import Project** (GitHub → Ozdemir-Makine-Web).
- **Env değişkenleri** ekle (Settings → Environment Variables). Değerleri `.env.local`'den KOPYALA (token'ı sohbete yazma):
  - `NEXT_PUBLIC_SANITY_PROJECT_ID` = qgzvu8g9
  - `NEXT_PUBLIC_SANITY_DATASET` = production
  - `NEXT_PUBLIC_SANITY_API_VERSION` = 2025-01-01
  - `SANITY_API_READ_TOKEN` = (`.env.local`'deki değer — **Sensitive** işaretle)
  - `NEXT_PUBLIC_SITE_URL` = https://ozdemirmakine.com.tr
- Deploy → **`…vercel.app`** URL'inde test. (⚠️ Gerçek domain cutover'ı DNS değiştirir, eski siteyi kapatır → o AYRI/sonraki karar, aceleye getirme. Önce vercel.app'te doğrula.)

### 3) Sanity CORS — Talha (sanity.io/manage)
- Proje → **API → CORS origins → Add**: `https://<proje>.vercel.app` + (hazır olunca) `https://ozdemirmakine.com.tr`. "Allow credentials" açık.
- Yoksa canlıda kartlar boş gelir (401).

### 4) Webhook — ben (kod) + Talha (Sanity webhook)
- Ben `app/api/revalidate/route.ts` + `SANITY_WEBHOOK_SECRET` yazarım (tag: machine/post/gallery/settings).
- Talha: Sanity → **API → Webhooks → Create**: URL `https://…/api/revalidate`, secret aynı. → Publish'te site **anında** tazelenir (60 sn beklemeden).

### 5) Gerçek içerik testi — Talha (Studio), ben doğrularım
- 1-2 makineye **gerçek foto + PDF** yükle → kartta/detayda gör.
- Galeri'ye birkaç **fuar fotosu** ekle → masonry + lightbox test.
- **Site Ayarları**'nı gözden geçir (telefon/adres/sosyal doğru mu).
- 1 blog yazısını **gerçek içerikle** doldur (zengin editör).
- Canlıda kontrol → Faz 7 KAPANIR.

## Sonraki fazlar (Sanity paketlendikten sonra)
- **Markalar → Sanity** (logo referansı; makine markası dropdown → referansa yükseltme).
- **Zoho form entegrasyonu** (iletişim/teklif formu → Zoho CRM; gizli alanlar `makineKodu/leadSource/kampanya` zaten hazır). `zoho-api` skill devrede.

## ORTAM NOTLARI
- **autocheck.cjs hook BOZUK** (yol boşluğu → sahte `'C:\Software' is not recognized`). YOKSAY. Elle: `npx tsc --noEmit` + `npx eslint <dosya>`.
- Dev: `npm run dev` → localhost:3000. `/studio` gömülü.
- **`.env.local` ASLA commit edilmez** (Sanity token — Editor yetkili). Token bir kez sohbette sızıp rotate edildi.
- Graphify: toplu değişiklikte `graphify update .` (LLM'siz, token'sız) → `.planning/graphs/`'a kopyala. Sorgu: `gsd-tools graphify query <terim>`.
- GitHub: `Tguleryuz52/Ozdemir-Makine-Web`, branch `feat/homepage-sections-and-project-hardening` push'lu, temiz.
